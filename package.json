{
  "name": "green-api-forwarder",
  "version": "1.0.0",
  "description": "Forward messages from one WhatsApp group to another via Green API using fetch",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
